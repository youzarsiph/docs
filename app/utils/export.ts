/**
 * Doc export utils
 * @param format string
 * @param fileName string
 */

const exportDoc = (format: string, fileName: string) => {
  switch (format) {
    case "format":
      break;

    default:
      break;
  }
};

export default exportDoc;
